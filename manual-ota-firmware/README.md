# Manual OTA firmware

This folder contains the current app-only firmware file for manual OTA update through the device
Web UI.

Local project path on MINIPC: `/home/msminipc/Arduino/Birdnetmic`.

Open the device update page:

```text
http://<device-ip>/ota
```

Choose **Upload compiled file** and select:

```text
firmware-app.bin
```
